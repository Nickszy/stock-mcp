# Streamlit configuration for Docker

[server]
port = 8501
address = "0.0.0.0"
headless = true
runOnSave = true
fileWatcherType = "poll"
enableCORS = false

[browser]
gatherUsageStats = false
serverAddress = "0.0.0.0"

[theme]
base = "light"
primaryColor = "#1f77b4"
